import React from 'react'
import ReactDOM from 'react-dom/client'

ReactDOM.createRoot(document.getElementById('vanilla-root')!).render(
    <React.StrictMode>
        Vanilla JS
    </React.StrictMode>,
)
